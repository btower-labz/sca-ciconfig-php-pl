pipeline {
    agent any 
    stages {
        stage('syntax') { 
            steps { 
                echo "syntax"
            }
        }
        stage('process'){
            steps {
                echo "process"
            }
        }
        stage('deploy') {
            steps {
                echo "deploy"
            }
        }
    }
}